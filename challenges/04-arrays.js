/* Desafio 04: Arrays - Filtrar Pares
Do array, filtre apenas os pares. Output: 2,4,6
curso javascriptdozero! */

const numbers = [1,2,3,4,5,6,7];
const pares = [];

// LACUNA: Use forEach ou for para push só pares em 'pares'
numbers.forEach(function(num) {
  // PREENCHA: if (num % 2 === 0) pares.push(num);
});

console.log(pares.join(','));

// Natan de Souza Cunha - Software Engineering 2026
